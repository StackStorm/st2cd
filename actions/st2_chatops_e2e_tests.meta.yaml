---
name: st2_chatops_e2e_tests
description: ChatOps end-to-end tests with all chat providers
enabled: true
runner_type: orquesta
entry_point: workflows/st2_chatops_e2e_tests.yaml
parameters:
  host_ip:
    type: string
  env:
    type: object

  chatops_e2e_tests_slack_channel_u16:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_channel_u16 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_username_u16:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_username_u16 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_api_token_u16:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_api_token_u16 }}"
    secret: true
    immutable: true
  chatops_e2e_tests_slack_user_username_u16:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_username_u16 }}"
    immutable: true
  chatops_e2e_tests_slack_user_api_token_u16:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_api_token_u16 }}"
    secret: true
    immutable: true

  chatops_e2e_tests_slack_channel_u18:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_channel_u18 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_username_u18:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_username_u18 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_api_token_u18:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_api_token_u18 }}"
    secret: true
    immutable: true
  chatops_e2e_tests_slack_user_username_u18:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_username_u18 }}"
    immutable: true
  chatops_e2e_tests_slack_user_api_token_u18:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_api_token_u18 }}"
    secret: true
    immutable: true

  chatops_e2e_tests_slack_channel_u20:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_channel_u20 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_username_u20:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_username_u20 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_api_token_u20:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_api_token_u20 }}"
    secret: true
    immutable: true
  chatops_e2e_tests_slack_user_username_u20:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_username_u20 }}"
    immutable: true
  chatops_e2e_tests_slack_user_api_token_u20:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_api_token_u20 }}"
    secret: true
    immutable: true

  chatops_e2e_tests_slack_channel_el7:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_channel_el7 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_username_el7:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_username_el7 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_api_token_el7:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_api_token_el7 }}"
    secret: true
    immutable: true
  chatops_e2e_tests_slack_user_username_el7:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_username_el7 }}"
    immutable: true
  chatops_e2e_tests_slack_user_api_token_el7:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_api_token_el7 }}"
    secret: true
    immutable: true

  chatops_e2e_tests_slack_channel_el8:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_channel_el8 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_username_el8:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_username_el8 }}"
    immutable: true
  chatops_e2e_tests_slack_bot_api_token_el8:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_bot_api_token_el8 }}"
    secret: true
    immutable: true
  chatops_e2e_tests_slack_user_username_el8:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_username_el8 }}"
    immutable: true
  chatops_e2e_tests_slack_user_api_token_el8:
    type: string
    default: "{{ st2kv.system.chatops_e2e_tests_slack_user_api_token_el8 }}"
    secret: true
    immutable: true
